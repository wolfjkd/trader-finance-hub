# 2026-06-24 工作日志

## 文档与代码一致性修复（4个高优先级问题）✅

### 问题1: README.md 版本/工具数与代码不一致 ✅

### 问题1: README.md 版本/工具数与代码不一致 ✅
- 标题 badge "57 个工具" → "61 个工具"
- 架构图 "AKShare 48 工具" → "56 工具"（含 signal_data 的 AKShare 部分）
- 数据源描述：拆分为 AKShare 50 + eltdx 5 + 东财/同花顺直连 6
- 版本历史：新增 v2.3.0 条目
- signal_data 章节标题 "14 个" → "15 个"（含 list_technical_indicators）

### 问题2: architecture.md 架构文档严重过时 ✅
- 全面重写，替换旧的"腾讯接口P0/Wind MCP/ftshare"数据源表
- 更新为实际架构：AKShare(主) + eltdx + 东财/同花顺直连
- 补充 astock_signals 14 个模块清单
- 补充 eltdx 通达信协议数据对比表
- 补充老板网络约束说明

### 问题3: signal_data.py 内部注释不一致 ✅
- "57 tools overall" → "61 个"
- 工具编号从 43-55（缺 list_technical_indicators）→ 48-61（14个全列出）
- 函数名与实际注册名一致（如 get_northbound_flow_signal）
- 注释补充 _signal 后缀说明

### 问题4: astock_signals/__init__.py 模块清单不全 ✅
- "模块清单（11个）" → "模块清单（14个）"
- 补充 smart_router / tick_store / ws_server 3 个基础设施模块

## 代码层面修复（后续追加）

### 问题5: signal_data.py 语法错误（致命） ✅
- 文件头有两个连续的 `"""`（line 24 和 line 25），导致整个文件被 Python 解析为一个巨大的字符串字面量
- 修复：删除多余的 `"""`，文件恢复正常
- 此 bug 导致 cn-financial-mcp 无法启动，影响全部 61 个 MCP 工具

### 问题6: pyproject.toml 缺少依赖声明 ✅
- `requests` 和 `stockstats` 是运行时必需但未声明在 dependencies 中
- stockstats 用于 indicators 模块计算技术指标，requests 用于 anti_ban_client/concept/hot_money/northbound
- websockets 用于 ws_server（可选），放入 optional [ws]
- 修复：dependencies 加 requests>=2.28.0 + stockstats>=0.6.0，optional 加 [ws]

### 问题7: config/mcp-servers.json 严重过时 ✅
- "tools": 42 → 61
- modules 列表缺 signal_data 和 eltdx_data
- routing 引用了已废弃的 tencent-http/ftshare/wind-mcp 作为主力源
- 修复：全面更新为当前实际架构

### 问题8: scripts/health_check.py 引用废弃数据源 ✅
- 删除 ftshare/Wind MCP/旧东财条目
- 新增 6 个实际数据源：AKShare东财/腾讯行情/push2delay/datacenter/同花顺hexin/cn-financial-mcp
- 修复 test_http 缺 User-Agent 导致同花顺返回 404
- 修复 test_command 在 Windows 上的编码和 shell 转义问题
- 改用临时脚本 + venv python 直接调用，避免 shell=True 的引号地狱
- 健康检测结果：6/6 全部通过

## 2026-06-24 下午补充修复

### 问题9: cn-financial-mcp/README.md 严重过时 ✅
- 工具数量从42个更新为61个
- 添加signal_data和eltdx_data模块说明
- 更新项目结构，添加tools/signal_data.py和tools/eltdx_data.py
- 更新版本信息和致谢部分
- 添加新的数据源fallback机制说明

### 问题10: cn-financial-mcp/tests/test_server.py 测试过时 ✅
- 测试方法名从test_all_42_tools_registered改为test_all_61_tools_registered
- 更新工具计数从42到61
- 添加对signal_data和eltdx_data工具的测试
- 更新版本工具计数验证（8+12+14+8+14+5=61）

### 问题11: README.md数据源描述不准确 ✅
- AKShare工具数从50个修正为56个（42基础+14信号数据中的AKShare部分）
- 保持与架构图中"AKShare封装（56工具）"一致

## 代码质量深度修复（12个问题）

### 致命（2个）
- **anti_ban_client.py**: 添加 `threading.Lock` 保护限流逻辑和 Session 初始化，双重检查锁
- **tick_store.py**: 添加 `_write_lock` 保护所有写操作，`total_changes` → `cursor.rowcount`，表名白名单校验防 SQL 注入

### 高风险（5个）
- **ws_server.py**: 默认绑定 127.0.0.1，添加 token 认证（WS_TOKEN 环境变量），客户端需先 auth 才能订阅
- **hot_money.py**: http:// → https://（同花顺支持 HTTPS）
- **tick_store.py**: SQL 注入防护（`_SAFE_TABLE_RE` 白名单校验）
- **concept.py**: `lru_cache(maxsize=1)` 替换为手动 TTL 缓存（1小时过期）
- **cache.py**: 添加 `threading.Lock` + `OrderedDict` LRU 淘汰（max_size=5000）

### 中等（5个）
- **dragon_tiger.py**: 3 处裸 `except Exception: pass` → 加 `logger.debug`
- **anti_ban_client.py**: `em_reset_session` 的裸 except 加日志
- **convertible_bond.py**: 子串匹配 → 精确匹配 + `df.loc[:, ~df.columns.duplicated()]` 去重
- **anti_ban_client.py**: `em_datacenter` 添加 403 状态码自动重试 + `r.raise_for_status()`
- **smart_router.py**: register 时即创建 SourceHealth，修复 health=None 不记录评分的 bug

### 低风险
- **__init__.py / signal_data.py**: 百度PAE 过时注释清理

### 验证结果
- 69 测试全部通过，0 warnings（可转债列名警告消除）
- 健康检测 5/6（push2.eastmoney.com 被老板网络封禁属已知，push2delay 正常）
- 所有 Python 源文件语法检查通过
